#include <stdio.h>
/**
 * main - a program that prints single digit numbers
 * Return: 0 success
 */
int main(void)
{
	int b;

	for (b = 0; b <= 10; b++)
		printf("%d", b);
	printf("\n");
	return (0);
}
